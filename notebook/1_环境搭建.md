# 环境搭建

直接参考作者的环境配置，使用 `python 3.10.1` 

# 分析SFTDataset
## 问题1:为什么要构造SFT专门的数据集
回答：因为原始的数据集格式对于不能直接输入到模型中，它会有一个对话模版，按照模版给的格式来进行输入。
为啥需要使用模版：
因为大模型本身只会接受一串token，它并不知道什么是 system / user / assistant，模板是“把人类对话翻译成模型能懂的语言”的协议。
如果你给他一个原始文本，它会把这些当成一个整体文本进行理解，它不知道谁是问题，谁是答案。
但如果给了它模版，它就能确定句子的结构，知道从哪里开始回答问题。
比如：原始数据：
```txt
 {"messages": [{"role": "system", "content": "你是个有用无害的助手"}, {"role": "user", "content": "告诉我明天的天气"}, {"role": "assistant", "content": "明天天气晴朗"}]}
```
使用模版后：
```txt
<|im_start|>system
你是个有用无害的助手
<|im_end|>
<|im_start|>user
告诉我明天的天气
<|im_end|>
<|im_start|>assistant
明天天气晴朗
<|im_end|>
```
qwen模型使用`<|im_start|>`、`<|im_end|>` 这种特殊的token来表示句子的开始和接受。
用system来表示规则、user表示问题，assistant表示答案。
这样模型在读取到<|im_start|>assistant之后，就知道接下来是自己要回答的内容了，就会按照指令回答问题，不然就会跟预训练一样继续按照用户问题续写了，而不知道回答问题。

## 问题2:如何构造
```py

class SFTDataset(Dataset):
    def __init__(self, jsonl_path, tokenizer, max_length=1024):
        super().__init__()
        # 模型的分词器
        self.tokenizer = tokenizer
        # 数据分词后的最大token数
        self.max_length = max_length
        # 当前数据集
        self.samples = load_dataset('json', data_files=jsonl_path, split='train')
        # <|im_start|>assistant
        self.bos_id = tokenizer(f'{tokenizer.bos_token}assistant\n', add_special_tokens=False).input_ids
        # <|im_end|>
        self.eos_id = tokenizer(f'{tokenizer.eos_token}\n', add_special_tokens=False).input_ids

    def __len__(self):
        return len(self.samples)

    # 创建聊天模版
    def create_chat_prompt(self, cs):
        messages = cs.copy()
        tools = cs[0]["functions"] if (cs and cs[0]["role"] == "system" and cs[0].get("functions")) else None
        return self.tokenizer.apply_chat_template(
            messages,
            tokenize=False,
            add_generation_prompt=False,
            tools=tools
        )

    # 生成标签
    def generate_labels(self, input_ids):
        # input_ids 就是一整串token
        # 只有答案的部分才会被设置为标签，其他都是-100，-100代表不计算损失
        labels = [-100] * len(input_ids)
        i = 0
        # 遍历每一个token
        while i < len(input_ids):
            # 如果是回答
            if input_ids[i:i + len(self.bos_id)] == self.bos_id:
                # start：回答的起始token
                start = i + len(self.bos_id)
                # end：回答的终止token
                end = start
                while end < len(input_ids):
                    if input_ids[end:end + len(self.eos_id)] == self.eos_id:
                        break
                    end += 1
                # labels 的部分的标签设置成原始toekn，需要计算loss
                for j in range(start, min(end + len(self.eos_id), self.max_length)):
                    labels[j] = input_ids[j]
                i = end + len(self.eos_id) if end < len(input_ids) else len(input_ids)
            else:
                i += 1
        return labels
    def __getitem__(self, index):
        # 当前数据
        sample = self.samples[index]
        # 当前数据的模版格式
        prompt = self.create_chat_prompt(sample['conversations'])
        # 将数据分词，不能超过max_length，超过就设置为pad_token_id(遇到这里就停止)
        input_ids = self.tokenizer(prompt).input_ids[:self.max_length]
        input_ids += [self.tokenizer.pad_token_id] * (self.max_length - len(input_ids))
        labels = self.generate_labels(input_ids)
        # # === 调试打印 ===
        # print(f"\n--- Sample {index} ---")
        # for i, (x, y) in enumerate(zip(input_ids[:-1], labels[1:])):
        #     print(f"{i:3d}: X={self.tokenizer.decode([x])!r:16s} ---> Y={self.tokenizer.decode([input_ids[i+1]])!r:16s} label={y}")
        # # ================
        return torch.tensor(input_ids, dtype=torch.long), torch.tensor(labels, dtype=torch.long)

```

